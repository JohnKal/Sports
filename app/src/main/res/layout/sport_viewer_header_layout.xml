<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/navy_blue"
    android:clickable="true"
    android:focusable="true">

    <ImageView
        android:id="@+id/icSport"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_marginStart="10dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:srcCompat="@drawable/ic_football" />

    <TextView
        android:id="@+id/tvSport"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:padding="12dp"
        android:textAppearance="@style/TextAppearance.AppCompat.Medium"
        android:textColor="@color/white"
        app:layout_constrainedHeight="true"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/icExpand"
        app:layout_constraintStart_toEndOf="@+id/icSport"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Example of item Header" />

    <ImageView
        android:id="@+id/icExpand"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:rotation="180"
        app:layout_constraintBottom_toBottomOf="@+id/tvSport"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@+id/tvSport"
        app:srcCompat="@drawable/ic_arrow_up_24" />
</androidx.constraintlayout.widget.ConstraintLayout>